export const DEFAULT_SLUG_LENGTH = 7;
export const MAX_SLUG_ATTEMPTS = 8;

export const DEFAULT_RATE_LIMIT_WINDOW_SECONDS = 60;
export const DEFAULT_RATE_LIMIT_MAX_REQUESTS = 20;

export const DEFAULT_LINK_TTL_SECONDS = 0;

export const RATE_LIMIT_PREFIX = 'rate-limit:shorten';
